Statically compiled Perl 5.12 cannot load POSIX, thus we inline
this code from bits/wexitstatus.h.

diff -aur automake-1.11.orig/lib/Automake/FileUtils.pm automake-1.11/lib/Automake/FileUtils.pm
--- automake-1.11.orig/lib/Automake/FileUtils.pm	2011-04-12 17:36:05.799295156 +0000
+++ automake-1.11/lib/Automake/FileUtils.pm	2011-04-12 17:37:41.024187999 +0000
@@ -262,20 +262,18 @@
     }
   else
     {
-      use POSIX qw (WIFEXITED WEXITSTATUS WIFSIGNALED WTERMSIG);
-
-      if (WIFEXITED ($?))
+      if (!($? & 0x7f))
 	{
-	  my $status = WEXITSTATUS ($?);
+	  my $status = ($? & 0xff00) >> 8;
 	  # Propagate exit codes.
 	  fatal ('',
 		 "$command failed with exit status: $status",
 		 exit_code => $status)
 	    unless $status == $expected;
 	}
-      elsif (WIFSIGNALED ($?))
+      elsif (((($? & 0x7f) + 1) >> 1) > 0)
 	{
-	  my $signal = WTERMSIG ($?);
+	  my $signal = $? & 0x7f;
 	  fatal "$command terminated by signal: $signal";
 	}
       else
